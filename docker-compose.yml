services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DB_PATH: /data/db.sqlite
    volumes:
      - nutritracker-data:/data
      - ./logs:/app/logs

volumes:
  nutritracker-data:
